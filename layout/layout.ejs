<!DOCTYPE html>
<html lang="zh-CN">
  
  <%- partial('_partial/head') %>
  
  <body class="line-numbers">
  
  <div class="pgbar">
    <div class="bar-in"></div>
  </div>
  <script src="/js/layouts@20200929.js"></script>
  <!-- <script id="header-js"></script>
  <script type="text/javascript">
    let headerJs = document.getElementById('header-js');
    if (screen.width < 768) {
      headerJs.setAttribute('src', '');
    } else {
      headerJs.setAttribute('src', '/js/layouts@20200921.js');
    }
  </script> -->
<!-- <body onload="prettyPrint();" class="line-numbers"> 旧款代码高亮 -->
  <% if (!is_home()) { %>
    <%- partial('_partial/header') %>
  <% } %>
    <div class="wrap clearfix">
      <% if (!is_post() && page.type != 'tags' && page.type != 'note') { %>
        <div class="banner-back" style="position: relative;">
          <div class="bannerBg" style="position: fixed; top: 0; left: 0; right: 0; height: 280px; background: #25566b url('<%- theme.bannerBg %>') center 50% no-repeat; background-size: cover;"></div>
        </div>
      <% } %>
  <% if (is_post() || page.type == 'tags' || page.type == 'note'){ %>
  <div class="main" style="padding-top: 57px; box-shadow: initial; background-color: initial; backdrop-filter: initial;-webkit-backdrop-filter: initial;">
  <% } else if (is_home()) { %>
    <div class="main" style="background-color: #fff;">
  <% } else { %>
    <div class="main">
  <% } %>
    <div class="wrap-content clearfix">
      <% if (page.type != 'note' && page.type != 'spage' && !is_archive()) { %>
      <div class="bar barbar">
        <!-- <div class="barside"> -->
        <!-- S 侧栏插件 -->
        <% if (is_home() && theme.showAuthor){ %>
        <%- partial('_partial/author') %>
        <% } %>

        <!-- 最近文章 -->
        <% if (!is_home() && theme.showRecent){ %>
        <%- partial('_partial/recent') %>
        <% } %>

        <!-- <hr style="border: 0;border-top: 1px solid #eaecef;"> -->

        <!-- 分类 -->
        <% if (!is_home() && page.type != 'categories' && !is_post() && theme.showCategory){ %>
          <%- partial('_partial/category') %>
        <% } %>
          
        <!-- <hr style="border: 0;border-top: 1px solid #eaecef;"> -->
          
        <!-- 归档 -->
        <% if (!is_home() && theme.showArchive){ %>
          <%- partial('_partial/archive') %>
        <% } %>
            
        <!-- 标签 -->
        <% if (!is_home() && page.type != 'tags' && !is_post() && theme.showTag){ %>
          <%- partial('_partial/tag') %>
        <% } %>

        <!-- 文章目录 -->
        <% if (is_post() && theme.showPostMenu){ %>
          <%- partial('_partial/menu') %>
        <% } %>
        <!-- E 侧栏插件 -->
        </div>
        <% } %>
        <!-- <%- theme.notice %> -->
        <%- body %>
      </div>
    </div>
    </div>
    <% if (!is_home() && page.type != 'note'){ %>
      <%- partial('_partial/footer') %>
    <% } %>
  <script id="layout-js"></script>
  <script type="text/javascript">
    let layoutJs = document.getElementById('layout-js');
    if (screen.width < 1236) {
      layoutJs.setAttribute('src', '/js/layout-m20200920.js?v=0421');
    }
  </script>
</body>

</html>